# Delta Exchange Environment Configuration
# Options: india_prod, global_prod, india_testnet
DELTA_ENV=global_prod

# Delta API Credentials
DELTA_API_KEY=your_api_key_here
DELTA_API_SECRET=your_api_secret_here

# Optional: Override Base URL (if not using one of the presets)
# DELTA_BASE_URL=https://api.delta.exchange

# Market Refresh & Validation Settings
# Minimum number of markets required in the dump
MIN_MARKETS=20

# Maximum allowed ratio of removed pairs in whitelist (0.0 - 1.0)
MAX_REMOVAL_RATIO=0.25

# Enforce strict volume checks (warn vs fail)
STRICT_VOLUME=false

# Filter Mode for Whitelist Generation
# Options: perps_usdt, all_futures, allowlist_regex
FILTER_MODE=perps_usdt

# Regex for allowlist_regex mode
ALLOWLIST_REGEX=.*USDT
