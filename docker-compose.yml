---
version: '3'
services:
  freqtrade:
    image: freqtradeorg/freqtrade:stable
    restart: unless-stopped
    container_name: freqtrade
    volumes:
      - "./user_data:/freqtrade/user_data"
    ports:
      - "127.0.0.1:8080:8080"
    env_file:
      - .env
    environment:
      - FREQTRADE__EXCHANGE__KEY=${DELTA_API_KEY}
      - FREQTRADE__EXCHANGE__SECRET=${DELTA_API_SECRET}
      # These variables are expected to be set in the shell context (via scripts/common.sh)
      # or manually in .env if not using the scripts.
      - FREQTRADE__EXCHANGE__CCXT_CONFIG__urls__api__public
      - FREQTRADE__EXCHANGE__CCXT_CONFIG__urls__api__private
      - FREQTRADE__EXCHANGE__CCXT_CONFIG__urls__www
    command: >
      trade
      --logfile /freqtrade/user_data/logs/freqtrade.log
      --db-url sqlite:////freqtrade/user_data/tradesv3.sqlite
      --config /freqtrade/user_data/configs/${FREQTRADE_CONFIG_FILE:-config.delta.dryrun.json}
      --config /freqtrade/user_data/pairlists/whitelist.delta.${DELTA_ENV:-india_testnet}.json
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/api/v1/ping"]
      interval: 30s
      timeout: 10s
      retries: 3
