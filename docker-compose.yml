---
version: '3'
services:
  freqtrade:
    image: freqtradeorg/freqtrade:stable
    restart: unless-stopped
    container_name: freqtrade
    volumes:
      - "./user_data:/freqtrade/user_data"
    ports:
      - "127.0.0.1:8080:8080"
    environment:
      # Inherit from shell (set by scripts/common.sh)
      - FREQTRADE__EXCHANGE__KEY
      - FREQTRADE__EXCHANGE__SECRET
      - FREQTRADE__EXCHANGE__CCXT_CONFIG__URLS__API__public
      - FREQTRADE__EXCHANGE__CCXT_CONFIG__URLS__API__private
      - FREQTRADE__EXCHANGE__CCXT_CONFIG__URLS__www
      # Pass through for debugging/logging
      - DELTA_ENV
    env_file:
      - .env
    # Default command used when running `docker compose up`
    # FREQTRADE_CONFIG_FILE is expected to be set by the runner script
    # We also include a separate whitelist config to allow easy updates
    command: >
      trade
      --logfile /freqtrade/user_data/logs/freqtrade.log
      --db-url sqlite:////freqtrade/user_data/tradesv3.sqlite
      --config /freqtrade/user_data/configs/${FREQTRADE_CONFIG_FILE:-config.delta.dryrun.json}
      --config /freqtrade/user_data/pairlists/whitelist.delta.json
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8080/api/v1/ping || exit 1"]
      interval: 1m
      timeout: 10s
      retries: 3
