services:
  freqtrade:
    image: freqtradeorg/freqtrade:stable
    restart: unless-stopped
    container_name: freqtrade
    volumes:
      - "./user_data:/freqtrade/user_data"
    ports:
      - "127.0.0.1:8080:8080"
    environment:
      - FREQTRADE__EXCHANGE__KEY=${DELTA_API_KEY}
      - FREQTRADE__EXCHANGE__SECRET=${DELTA_API_SECRET}
      - DELTA_ENV=${DELTA_ENV:-india_testnet}
      - DAILY_LOSS_LIMIT_PCT=${DAILY_LOSS_LIMIT_PCT:--0.05}
      - MIN_MARKETS=${MIN_MARKETS:-20}
      - MAX_REMOVAL_RATIO=${MAX_REMOVAL_RATIO:-0.25}
      - STRICT_VOLUME=${STRICT_VOLUME:-false}
      - FILTER_MODE=${FILTER_MODE:-perps_usdt}
      - ALLOWLIST_REGEX=${ALLOWLIST_REGEX}
      # This env var is passed to the container, can be used by scripts inside
      - FREQTRADE_CONFIG_FILE=${FREQTRADE_CONFIG_FILE:-config.delta.dryrun.json}
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8080/api/v1/ping || exit 1"]
      interval: 1m
      timeout: 10s
      retries: 3
    # We use the config file specified in the environment variable.
    # Docker Compose interpolates this string.
    command: >
      trade
      --logfile /freqtrade/user_data/logs/freqtrade.log
      --db-url sqlite:////freqtrade/user_data/tradesv3.sqlite
      --config /freqtrade/user_data/configs/${FREQTRADE_CONFIG_FILE:-config.delta.dryrun.json}
      --config /freqtrade/user_data/pairlists/whitelist.delta.json
      --strategy ${FREQTRADE_STRATEGY:-DeltaSafeStrategy}
